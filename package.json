{
  "name": "booking-widget",
  "version": "1.0.0",
  "description": "Embeddable meeting booking widget — vanilla JS, multi-brand",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
