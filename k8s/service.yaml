apiVersion: v1
kind: Service
metadata:
  name: booking-widget-service
  labels:
    app: booking-widget
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: booking-widget
