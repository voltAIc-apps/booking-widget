apiVersion: v1
kind: Service
metadata:
  name: meetly-web
  namespace: meetly
  labels:
    app: meetly
    component: web
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: meetly
    component: web
